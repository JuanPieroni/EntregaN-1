//todo

//ToDo agregar middleware distintos roles

Regex y Restricción de Parámetros

router modular.
 
 ruteo avanzado

 router customizado
estandarizar respuestas de succes , error , message , server error etc.
manejo sistemizado de respuestas 
Gestion de middleware interiorizado: generar middleware a nivel de routers. (opc creo)

expresiones regulareS ? 

creo customRouter.js
agragar al customrouter el manejo de try catch mapeado y asi aplicarlo a todas las rutas

agregar luego regex en el customRouter

terminar de configurar el commander + dotenv

Listeners , child-process



process.on 
process.exit
sirve para que un proceso se ejecute mientras otro sigue ejecutando. 
child_process.fork
lo vamos a usar cuando veamos clusters 



-----------

Capa de peristencia ( managers ?)
Capa de servicio ( adicional ) intermedia entre estos dos 
Capa de negocio o controlador
-------
Capas adicionales para node.js ( router )

----

Flujo 

el sevicio se conececta con el manager ( importa a manager )
controller se comunica con  servicio. importa findall findByid, createone etc
el router llama al controlador. 

// Router → Controller → Manager/Service → Model

// Más capas, mejor separación
Router → Controller → Manager → DAO → Model

Loggear y documentar 

Singleton

server side rendering 
client side rendering 

headers de la peticion 
pasan token formato de envios de datos , identificador del elemento que hace la peticion.

datos a enviar :
metodos
query params 
body
route params

Cors